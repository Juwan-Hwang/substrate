[package]
name = "substrate-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core domain logic for Aevum — graph algorithms, content processing, experiment runtime"

[dependencies]
