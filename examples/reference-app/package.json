{
  "name": "@example/reference-app",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@substrate-platform/contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "@types/node": "^22.0.0"
  }
}
