{
  "name": "@substrate-platform/edge",
  "version": "0.2.0-canary.4",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json && bun run ../../scripts/fix-import-extensions.ts dist",
    "lint": "biome check src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "dependencies": {
    "@substrate-platform/contracts": "workspace:*",
    "@substrate-platform/db": "workspace:*",
    "hono": "^4.6.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.5",
    "@upstash/redis": "^1.34.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "miniflare": "^3.20250101.0"
  },
  "files": [
    "dist/**",
    "README.md",
    "LICENSE"
  ]
}
